[pytest]
markers =
    integration: marks tests that hit external systems like BigQuery
    warehouse: marks local warehouse/data-quality tests
    etl: marks ETL/unit-style transformation tests
addopts = --strict-markers