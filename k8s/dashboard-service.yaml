apiVersion: v1
kind: Service
metadata:
  name: retail-dashboard-service
spec:
  selector:
    app: retail-dashboard
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
  type: NodePort
