name: 'retail_analytics_pipeline'
version: '1.0.0'
config-version: 2

profile: 'retail_analytics_pipeline'

model-paths: ['models']
target-path: 'target'
clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  retail_analytics_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
